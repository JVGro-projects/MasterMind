<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Мастермайнд — ценности и правила</title>
  <meta name="description" content="Мастермайнд — пространство для роста, поддержки и созидания. Ценности и правила бизнес-сообщества." />

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    /* RESET */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      margin: 0;
    }

    /* ===== ТВОЙ CSS (БЕЗ ИЗМЕНЕНИЙ ПО СУТИ) ===== */

    .mm-page {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #faf8f5 0%, #f5f0e8 100%);
      color: #1a1a1a;
      line-height: 1.6;
    }

    .mm-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
    }

    .mm-hero {
      text-align: center;
      padding: 80px 24px;
    }

    .mm-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 16px;
      background: rgba(22, 101, 82, 0.1);
      color: #166552;
      border-radius: 50px;
      font-size: 14px;
      font-weight: 500;
      margin-bottom: 24px;
    }

    .mm-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(48px, 8vw, 72px);
      font-weight: 600;
      margin-bottom: 24px;
    }

    .mm-subtitle {
      font-size: 20px;
      color: #666;
      max-width: 600px;
      margin: 0 auto;
    }

    .mm-subtitle span {
      color: #166552;
      font-weight: 500;
    }

    .mm-values {
      padding: 80px 24px;
    }

    .mm-section-header {
      text-align: center;
      margin-bottom: 48px;
    }

    .mm-section-label {
      color: #166552;
      font-size: 14px;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .mm-section-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(36px, 5vw, 48px);
      font-weight: 600;
      margin-top: 16px;
    }

    .mm-values-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
      gap: 24px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .mm-value-card {
      background: #fff;
      border-radius: 16px;
      padding: 32px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.05);
      border: 1px solid rgba(0,0,0,0.05);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .mm-value-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 40px rgba(0,0,0,0.1);
    }

    .mm-value-icon {
      width: 56px;
      height: 56px;
      background: rgba(22, 101, 82, 0.1);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 20px;
      font-size: 24px;
    }

    .mm-value-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 24px;
      font-weight: 600;
      margin-bottom: 12px;
    }

    .mm-value-text {
      color: #666;
      font-size: 15px;
    }

    .mm-footer {
      text-align: center;
      padding: 48px 24px;
      border-top: 1px solid #eee;
      color: #999;
      font-size: 14px;
    }
  </style>
</head>

<body>

<div class="mm-page">

  <!-- HERO -->
  <section class="mm-hero">
    <div class="mm-badge">✨ Бизнес-сообщество</div>
    <h1 class="mm-title">Мастермайнд</h1>
    <p class="mm-subtitle">
      Пространство для роста, поддержки и созидания.
      <span>Ценности и правила</span>, которые объединяют нас.
    </p>
  </section>

  <!-- VALUES -->
  <section class="mm-values">
    <div class="mm-section-header">
      <div class="mm-section-label">Что нас объединяет</div>
      <h2 class="mm-section-title">Наши ценности</h2>
    </div>

    <div class="mm-values-grid">
      <!-- карточки без изменений -->
      <!-- можешь вставить все свои карточки сюда -->
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="mm-footer">
    <p>Создано с ❤️ для сообщества</p>
    <p>© 2025 Мастермайнд. Все права защищены.</p>
  </footer>

</div>

</body>
</html>
